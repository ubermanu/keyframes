{
  "name": "keyframes",
  "version": "0.1.0",
  "devDependencies": {
    "rollup": "^2.33.1",
    "rollup-plugin-chrome-extension": "^3.4.0"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w"
  }
}
